kind = "web"
  previewPath = "/"
  title = "Makinasul"
  version = "1.0.0"
  id = "artifacts/makinasul"
  router = "path"

  [[integratedSkills]]
  name = "react-vite"
  version = "1.0.0"

  [[services]]
  name = "web"
  paths = [ "/" ]
  localPort = 8080

  [services.development]
  run = "pnpm --filter @workspace/makinasul run dev"

  [services.production]
  build = [ "pnpm", "--filter", "@workspace/makinasul", "run", "build" ]
  publicDir = "artifacts/makinasul/dist/public"
  serve = "static"

  [[services.production.rewrites]]
  from = "/*"
  to = "/index.html"

  [services.env]
  PORT = "8080"
  BASE_PATH = "/"
  